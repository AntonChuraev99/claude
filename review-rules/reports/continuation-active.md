# Review-rules — продолжение (ACTIVE, отложено)

**Статус:** Active — deferred (взяться позже)
**Заведено:** 2026-07-15
**Контекст:** после аудита эффективности (`effectiveness-2026-07-15.md`). Улучшения выявлены,
сознательно отложены по решению владельца. НЕ completed.

Прим.: в этом репо (whitelist-публичный `~/.claude`) нет `docs/active/` — активная работа по системе
живёт рядом с самой системой в `review-rules/`. Приватная deferred-заметка продублирована в project-memory.

## Backlog (по приоритету)

### P1 — шум: зарезать 5 «кричащих» runtime-правил
5 правил → ~6757 срабатываний, 0 подтверждений (100% est-FP): `initjs-window-sentry-dead-channel` (2299×),
`boot-resource-retry-no-terminal-spinner-state` (1486×), `css-border-on-rounded-pill-artifacts` (1155×),
`double-system-bar-padding` (1039×), `textalign-center-without-fillwidth` (777×).
Топят сигнал L2-судьи.
- **Действие:** ужесточить `detect` каждого (сузить `has`, добавить `lacks`-guard на легитимный паттерн),
  либо понизить до advisory, не эскалируемого в L2.
- **Verify:** smoke-тест из README — правило ловит свой исходный инцидент, но молчит на чистом коде.

### P1 — метрика: вывести «confirmed-per-L2-run» в rollup
`stats.py` репортит только по-находочный FP (90%) → система выглядит хуже, чем есть.
Настоящая метрика ценности — **24% прогонов L2 с ловлей** — не выводится.
- **Действие:** в `render()` добавить строку в `## Выводы`: `L2 hit-rate: N/M прогонов с ≥1 confirmed (X%)`.
  Считается по `l2` events: доля прогонов, где `any(judged.verdict==confirmed)`.

### P2 — гигиена: smoke-прогнать 55 спящих правил
55/89 ни разу не срабатывали. Прогнать каждое на его `source`-инциденте: ловит ли вообще.
Мёртвые (regex-опечатка / устаревший glob) — починить/удалить; настоящие dormant — оставить как страховку.

### P2 — данные: логировать `armed → resolved` для L3
process-gate вооружался 64 раза, но исход не пишется (только `armed`). Не видно, сколько раз гейт
реально что-то поймал vs просто спросил.
- **Действие:** L3-событие дополнить полем `outcome` (resolved/nochange/caught) — главный проставляет
  в `/end-session` после ответа на process-вопрос.

### P3 — масштаб: поле `escalate:` в схеме правила
Не каждое runtime-правило достойно дорогого L2. Ввести `escalate: true/false` — шумные остаются WARN
в логе, но не будят судью. Прямое следствие находки о 90% шуме.
- **Действие:** `manifest.yaml` схема + фильтр в `review()` / в брифе L2.

### P3 — L2-агент: вписать ast-index в бриф `bug-pattern-reviewer`
Единственное место, где замечание «поиск через ast-index» реально применимо — L2 **читает код** для
верификации runtime-флагов (это code discovery, правило применимо). Сейчас в брифе агента ast-index нет
(есть только в CLAUDE.md + knowledge-scout). `run.py` (L1) остаётся на grep/glob — там ast не к месту
(config/CSS/gitignore/строковые литералы, zero-dep pre-commit контракт).
- **Действие:** добавить в `agents/bug-pattern-reviewer.md` инструкцию ast-index для навигации при чтении кода.

## Definition of Done для возврата
- P1×2 закрыты (шум зарезан + метрика в rollup) — минимум.
- Каждое изменение правил прогнано smoke.
- `stats.py` перегенерил `review-rules.md` без регрессий.
